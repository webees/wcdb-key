# 编程规范（CODING STYLE）

本规范沉淀自 wcdb-key 项目的实际开发过程，适用于本项目全部 Rust 代码与文档。原则：**可读性 > 行数 > 巧技**，违反语义安全的"精简"一律拒绝。

## 1. 命名

| 对象 | 规则 | 示例 |
|---|---|---|
| 文件 / 模块 | snake_case，名实相符，禁用 `common`/`utils` 等弱名 | `shared.rs`、`mac.rs`、`win.rs`、`hook.py` |
| 函数 / 变量 | snake_case，动宾结构 | `collect_account_data`、`run_capture` |
| 常量 | SCREAMING_SNAKE_CASE | `KEY_ROUNDS`、`CIPHER_CONFIG_NAME` |
| 类型 | UpperCamelCase；错误类型以 `Error` 结尾（不用缩写 `Err`） | `Transaction`、`WxError` |
| CLI 旗标 | kebab-case | `--wechat-path` |
| 局部变量 | 禁止掩盖含义的双字母缩写 | `mac_key` 而非 `mk` |
| 临时文件 | 全称命名（排错时路径可见） | `capture_callback.py` 而非 `capture_cb.py` |
| 公开 API | 最小化：平台模块只暴露入口函数，其余一律私有或 `pub(crate)` | `run_capture` / `restore_and_report` |

短名仅在"领域内通用 + 高频调用"时豁免（如 shell 助手 `sh`、printf 家族 `sayf`/`failf`）。

## 2. 注释

- 全中文；句末用中文句号。
- 层级：`//!` 模块头 → `///` 公开项 rustdoc → `//` 内部说明。
- `///` 首行写行为摘要，机制细节进代码不进注释。
- 无法自解释的例外（如元错误必须保留字面量），用注释写明原因：
  `// ponytail: 这两个 expect 是元错误——注册表自身损坏时，无法再用注册表翻译自己的 panic 文本。`
- **跨语言算法重复例外**：`hook.py`（LLDB 回调）与 `crypto.rs` 的 page-1 HMAC 校验算法必须一致；LLDB 脚本接口仅支持 Python，无法复用 Rust 实现，该重复为架构必需——修改任一方须同步另一方，并在两处保留「需同步」注释。

## 3. 消息与错误：全局编号系统

- 所有用户可见的消息/错误带数字编号，**唯一注册表**为 `src/messages.json`（编译期 `include_str!` 嵌入）。代码内禁止内联文本。
- 编号 `> 0` 信息，`< 0` 错误。分段：`1–99/-1–-99` 通用与 CLI、`±100` 主流程、`±200` macOS、`±300` Windows、`-500` 底层命令/IO。
- 调用处只传数字：`say(105)`、`fail(-201)`、`failf(-105, &[…])`；`{0}`/`{1}` 为参数占位。
- 未知编号回退 `-505`（防递归：直接查表，不再调用自身）。
- 新增消息先注册再引用；已删除的消息必须同步删除调用点（编译器不查编号存在性）。
- **元错误例外**：注册表自身损坏时的 panic 无法用注册表翻译，保留字面量并注释说明。
- **静态帮助文本例外**：`USAGE` 帮助/版本文本为排版类静态文档（含版本与平台占位符），允许以 `const USAGE` 内联，不走注册表；其余运行期用户可见文本一律入注册表。
- i18n：只改 JSON，不动代码。

## 4. 输出通道与 JSON

- 人类模式：编号消息走 stdout，恢复/跳过等告警走 stderr。
- `--json` 模式：stdout 只出最终结果，日志与交互提示一律走 stderr；错误同样 JSON 化，退出码 1。
- JSON 统一三字段：`{"code": 编号, "data": 数据或 null, "msg": 对应消息}`。
- 交互提示（`ask`）在两种模式下都 flush 对应的流。

## 5. 错误处理

- 自定义编号错误实现 `std::error::Error`，经 anyhow 传播；调用方断言用 `downcast_ref::<WxError>().code` 精确校验编号，而非"报错了就算"。
- 重复的 `map_err(|e| fail_io(-501, e))` 收敛为扩展 trait（`IoCoded::coded()`），不引入依赖。
- 校验类流程（全库校验）为 all-or-nothing：任一子项失败整体拒绝；并行工作线程 panic 视为整体失败，绝不静默漏检。
- 快速失败：附加失败、断点未解析、备份无效等场景立即终止并恢复现场。

## 6. 资源与事务

- 需要清理的资源实现 `Drop` 兜底（调试器、临时目录）；主流程用显式 `finish()` 标记成功路径，Drop 只兜异常路径。
- Ctrl-C 处理器执行与 Drop 相同的恢复逻辑，退出码 130。
- 破坏性替换用"暂存克隆 + 原子交换"（rename），任何一步失败不得让系统处于破损状态。
- 敏感文件：落盘 0600、目录 0700、临时文件先写 `.tmp` 再 `rename`、`sync_all` 后交换。
- 捕获成功后 SIGKILL 临时进程，避免崩溃报告上报。

## 7. 安全红线（不可精简）

- 零网络请求。
- 不修改目标程序行为；目标进程在受控状态下终止，不产生崩溃上报。
- 密钥类数据只在结束时打印一次；真实信息（账号、密钥、路径）禁止进入仓库——测试数据走环境变量（`-- --ignored` 测试）。
- 深度验签（厂商 Team ID）在备份与恢复两个方向都要执行。

## 8. unsafe 与性能

- unsafe 收窄到 FFI 调用点，一个调用一个块；资源句柄用 Drop guard 管理。
- CPU 密集循环（PBKDF2 校验）按核数并行：`std::thread::scope`，零新增依赖。
- 依赖纪律：优先梯子为 stdlib → 平台 API → 已装依赖；为几行代码新增依赖禁止。

## 9. 测试分层

| 层 | 策略 |
|---|---|
| 纯逻辑（密码学、扫描过滤、消息渲染） | 必须单测：往返、拒绝、边界、错误编号断言 |
| 需要文件结构的函数 | 合成数据（`#[cfg(test)] pub(crate) mod synth` 构造带正确 HMAC 的假库）+ 临时目录（带 pid 隔离） |
| 真实账号端到端 | `#[ignore]` 测试 + 环境变量传入，真实数据不入库 |
| 系统交互（真实进程、调试器） | 不做单测，靠人工端到端验证 |

测试中的错误断言精确到编号：`err.downcast_ref::<WxError>().unwrap().code == -105`。

## 10. 流程门禁

每次提交前全绿：

```bash
cargo fmt
cargo build --release          # 0 错误 0 警告
cargo test                     # 全部通过
cargo clippy --release         # 0 警告（sort_by_key、is_multiple_of 等照改）
cargo check --target x86_64-pc-windows-msvc
```

- 发布配置：`strip = true`、`lto = true`、`codegen-units = 1`。
- 仓库历史：单提交，多次迭代用 `commit --amend` + force push；远端永远只有一个提交。
- 提交信息：固定格式 `🖕🐧 <时间戳>`，时间戳为提交时刻 `%Y%m%d%H%M`（如 `🖕🐧 202609010513`），每次提交取当时实际时间，禁止复用旧时间戳。
- 改动代码后同步更新 README（表格优先），并做脱敏自查（真实账号/密钥/用户名 grep 归零）。

## 11. 文档风格

- 能用表格用表格：选项、FAQ、故障排查、消息分段、安全设计、已验证范围。
- 去口水话与 AI 味：无破折号抒情收尾、无宣言式口号、无"这就是 X 的作用"式总结句；版本号与实测细节集中在一处（已验证范围），正文泛化表述。
- 不直接抄袭他人 README：参考思路须重写为原创表述并致谢。
- 输出示例中的真实数据一律脱敏（账号、密钥、用户名）。
